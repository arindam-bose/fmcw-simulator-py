# fmcw-simulator-py
This is a FMCW radar simulator written in Python

## Installation
```
$ git clone git@github.com:arindam-bose/fmcw-simulator-py.git
$ cd fmcw-simulator-py
$ virtualenv venv -p /usr/bin/python3
$ source venv/bin/activate
$ pip install -r requirements.txt
```
